#include <iostream>

using namespace std;

int main()
{
    int n, k;
    int w[1000], v[1000];
    int result[1000][1000];
    cin >> n >> k;
    for (int i = 0; i < n; i++)
    {
        cin >> w[i] >> v[i];
    }
    for (int x = 0; x < n; x++)
    {
        for (int j = 0; j < k; j++)
        {
                }
    }
}
